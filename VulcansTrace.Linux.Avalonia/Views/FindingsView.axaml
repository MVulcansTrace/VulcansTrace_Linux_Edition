<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:VulcansTrace.Linux.Avalonia.ViewModels"
             x:Class="VulcansTrace.Linux.Avalonia.Views.FindingsView"
             x:DataType="vm:FindingsViewModel">

    <Design.DataContext>
        <vm:FindingsViewModel/>
    </Design.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource VtTextSecondaryBrush}"/>
            <TextBox Text="{Binding SearchText}"
                     Width="220"
                     VerticalAlignment="Center"
                     Theme="{StaticResource VtInput}"
                     AutomationProperties.AutomationId="SearchTextBox"/>
            <TextBlock Text="Severity filter:" VerticalAlignment="Center" Margin="12,0,4,0" Foreground="{StaticResource VtTextSecondaryBrush}"/>
            <ComboBox ItemsSource="{Binding SeverityFilters}"
                      SelectedItem="{Binding SelectedSeverityFilter}"
                      Width="180"
                      VerticalAlignment="Center"
                      Theme="{StaticResource VtComboBox}"
                      AutomationProperties.AutomationId="SeverityFilterComboBox"/>
        </StackPanel>

        <WrapPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center" x:CompileBindings="False">
            <Button Content="Accept Risk"
                    Theme="{StaticResource VtSecondaryButton}"
                    Margin="0,0,8,6"
                    ToolTip.Tip="Mark the selected finding as accepted/expected"
                    Command="{Binding DataContext.AcceptRiskCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    AutomationProperties.AutomationId="FindingsAcceptRiskButton"/>
            <TextBlock Text="Select a finding to accept or remediate risk."
                       VerticalAlignment="Center"
                       Foreground="{StaticResource VtTextMutedBrush}"
                       FontSize="{StaticResource VtFontSizeSm}"
                       Margin="0,0,0,6"/>
        </WrapPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredItems}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserResizeColumns="True"
                  AutomationProperties.AutomationId="FindingsDataGrid"
                  AutomationProperties.Name="Findings Table">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Severity" Width="80" MinWidth="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Severity, Converter={StaticResource SeverityToBrushConverter}}"
                                    CornerRadius="4"
                                    Padding="6,2"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Margin="2">
                                <TextBlock Text="{Binding Severity}"
                                           Foreground="{Binding Severity, Converter={StaticResource SeverityToForegroundBrushConverter}}"
                                           FontWeight="SemiBold"
                                           FontSize="{StaticResource VtFontSizeSm}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Count" Binding="{Binding GroupBadge}" Width="80" MinWidth="80" />
                <DataGridTextColumn Header="Confidence" Binding="{Binding Confidence}" Width="*" MinWidth="90" />
                <DataGridTextColumn Header="Evidence Signals" Binding="{Binding EvidenceSignalsDisplay}" Width="1.5*" MinWidth="180" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*" MinWidth="100" />
                <DataGridTextColumn Header="Source" Binding="{Binding SourceHost}" Width="*" MinWidth="100" />
                <DataGridTextColumn Header="Target" Binding="{Binding Target}" Width="*" MinWidth="100" />
                <DataGridTextColumn Header="From" Binding="{Binding TimeStart, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="Auto" MinWidth="120" />
                <DataGridTextColumn Header="To" Binding="{Binding TimeEnd, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="Auto" MinWidth="120" />
                <DataGridTextColumn Header="MITRE ATT&amp;CK" Binding="{Binding MitreTechniquesDisplay}" Width="Auto" MinWidth="130" />
                <DataGridTextColumn Header="Description" Binding="{Binding ShortDescription}" Width="2*" MinWidth="180" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
